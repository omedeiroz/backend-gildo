# Exemplo de arquivo .env para o backend
# Copie este arquivo para `.env` e preencha os valores reais.

# String de conexão com o PostgreSQL (Neon, Heroku, local, etc.)
DATABASE_URL=postgresql://<user>:<password>@<host>:<port>/<dbname>?sslmode=require

# JWT - segredos usados para assinar tokens
JWT_SECRET=uma_chave_secreta_super_forte_aqui
JWT_REFRESH_SECRET=outra_chave_secreta_para_refresh_tokens

# Expirações (opcional) - exemplos: '15m', '1h', '7d'
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Porta do servidor
PORT=3000
